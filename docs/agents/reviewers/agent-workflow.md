# Agent Workflow and Lifecycle Review Overlay

Use with `../code_reviewer.md` for changes to `AGENTS.md`, focused agent docs, summaries/memory, worktree/database/resource
rules, review routing, verification helpers, or instruction evaluations.

Check authority ordering, task-need routing, short-prompt behavior, read-only versus change boundaries, preservation of
unrelated work, portable public policy, secret/value handling, exact resource identity, capability-conditional tools and
delegation, stale/duplicate owners, summary/memory promotion, benchmark leakage, representative real-work evidence,
protected regressions, and local stop criteria. Ensure the instructions remain useful in both the framework baseline and
copied derived applications without assuming branch, protection, release, upstream, or deployment behavior.

Reject wording-only churn without a concrete failure mode or measured cost. Treat benchmark claims as evidence only when
variants, task/code baselines, runtime/model/tool context, solution isolation, and adjudication are comparable.

Return only Confirmed, Needs validation, or Observation items using the common severity/location/impact/fix format.
